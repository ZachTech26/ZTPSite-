---
import Layout from '../layouts/Layout.astro';

const projects = [
  { title: 'Home Storage Server', desc: 'A home storage server running TrueNAS CE for SMB and Network location shares', status: 'live', tags: ['storage', 'networking'] },
    { title: 'Azure Lab Environment', desc: 'A sandbox subscription for practicing Azure administration end to end.', status: 'progress', tags: ['azure', 'cloud'] },
  { title: 'Home Network Segmentation', desc: 'Splitting a flat home network into VLANs for IoT, guest and trusted devices.', status: 'queued', tags: ['networking'] },
];
---

<Layout title="Projects — ZTP." description="Project write-ups from ZachsTechPage.">
  <section class="tight">
    <div class="wrap">
      <span class="eyebrow">&gt;ztp. current projects</span>
      <h1>Projects</h1>
      <p class="lede">Builds, labs and experiments, with notes on what worked, what broke and what I learned along the way.</p>
    </div>
  </section>

  <section class="tight">
    <div class="wrap">
      <div class="grid">
        {projects.map((p) => (
          <div class="card">
            <div class="card-title">{p.title}</div>
            <p class="card-desc">{p.desc}</p>
            <span class="status">
              <span class={`status-dot ${p.status}`}></span>
              {p.status === 'live' ? 'Live' : p.status === 'progress' ? 'In progress' : p.status === 'queued' ? 'Queued' : 'Planning'}
            </span>
            <div class="tags">
              {p.tags.map((t) => <span class="tag">{t}</span>)}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
</Layout>
