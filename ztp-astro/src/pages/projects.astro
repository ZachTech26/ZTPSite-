---
import Layout from '../layouts/Layout.astro';
import { projects, statusLabel } from '../data/projects.js';
---
<Layout title="Projects — ZTP." description="Project write-ups from ZachsTechPage.">
  <section class="tight">
    <div class="wrap">
      <span class="eyebrow">&gt;ztp. current projects</span>
      <h1>Projects</h1>
      <p class="lede">Builds, labs and experiments, with notes on what worked, what broke and what I learned along the way.</p>
    </div>
  </section>

  <section class="tight">
    <div class="wrap">
      <div class="grid">
        {projects.map((p) => (
          <a class="card" href={`/projects/${p.slug}`}>
            <div class="card-title">{p.title}</div>
            <p class="card-desc">{p.desc}</p>
            <span class="status">
              <span class={`status-dot ${p.status}`}></span>
              {statusLabel(p.status)}
            </span>
            <div class="tags">
              {p.tags.map((t) => <span class="tag">{t}</span>)}
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>
</Layout>