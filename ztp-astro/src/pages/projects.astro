---
import Layout from '../layouts/Layout.astro';

const projects = [
  { title: 'Home Network Segmentation', desc: 'Splitting a flat home network into VLANs for IoT, guest and trusted devices, with firewall rules between each.', status: 'live', tags: ['networking', 'security'] },
  { title: 'Azure Lab Environment', desc: 'A sandbox subscription for practicing Azure administration end to end, resource groups, IAM and monitoring.', status: 'progress', tags: ['azure', 'cloud'] },
  { title: 'Photo Media Pipeline', desc: 'An automated workflow for sorting, tagging and backing up photos across devices.', status: 'planning', tags: ['automation', 'photo media'] },
];
---
<Layout title="Projects — ZTP." description="Project write-ups from ZachsTechPage.">
  <section class="tight">
    <div class="wrap">
      <span class="eyebrow">&gt;ztp. current projects</span>
      <h1>Projects</h1>
      <p class="lede">Builds, labs and experiments, with notes on what worked, what broke and what I learned along the way.</p>
    </div>
  </section>

  <section class="tight">
    <div class="wrap">
      <div class="grid">
        {projects.map((p) => (
          <div class="card">
            <div class="card-title">{p.title}</div>
            <p class="card-desc">{p.desc}</p>
            <span class="status">
              <span class={`status-dot ${p.status}`}></span>
              {p.status === 'live' ? 'Live' : p.status === 'progress' ? 'In progress' : 'Planning'}
            </span>
            <div class="tags">
              {p.tags.map((t) => <span class="tag">{t}</span>)}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
</Layout>
